#pragma once

/******************************************************************************/

#define OK_OR_DIE(exp)                                                  \
do {                                                                    \
  if (!(exp).ok()) {                                                    \
    std::cerr << "DIE AT " << __FILE__ << ":" << __LINE__ << std::endl; \
    exit(1);                                                            \
  }                                                                     \
} while (0);

/******************************************************************************/
